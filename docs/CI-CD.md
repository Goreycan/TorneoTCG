# Integración continua

GitHub Actions automatiza la validación del código en eventos de integración. El workflow definido para la evaluación debe ejecutarse cuando se actualiza `develop` y cuando se crea o actualiza un Pull Request hacia `main`.

El objetivo es detectar errores de compilación antes de integrar cambios en la rama estable.